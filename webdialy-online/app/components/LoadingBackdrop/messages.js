/*
 * LoadingBackdrop Messages
 *
 * This contains all the text for the LoadingBackdrop component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.LoadingBackdrop';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the LoadingBackdrop component!',
  },
});
