/*
 * WaterMark Messages
 *
 * This contains all the text for the WaterMark component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.WaterMark';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the WaterMark component!',
  },
});
