/*
 * RenderField Messages
 *
 * This contains all the text for the RenderField component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.RenderField';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the RenderField component!',
  },
});
