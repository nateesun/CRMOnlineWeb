/*
 * GoogleMap Messages
 *
 * This contains all the text for the GoogleMap container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.GoogleMap';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the GoogleMap container!',
  },
});
