/*
 * LineLogin Messages
 *
 * This contains all the text for the LineLogin container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.LineLogin';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the LineLogin container!',
  },
});
