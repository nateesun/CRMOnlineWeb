// import { selectMainLayoutAppDomain } from '../selectors';

describe('selectMainLayoutAppDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
