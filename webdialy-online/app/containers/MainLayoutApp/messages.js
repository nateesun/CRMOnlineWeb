/*
 * MainLayoutApp Messages
 *
 * This contains all the text for the MainLayoutApp container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.MainLayoutApp';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MainLayoutApp container!',
  },
});
