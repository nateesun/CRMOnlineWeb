/*
 * MasterUI Messages
 *
 * This contains all the text for the MasterUI container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.MasterUI';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MasterUI container!',
  },
});
