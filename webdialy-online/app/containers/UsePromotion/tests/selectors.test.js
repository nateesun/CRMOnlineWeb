// import { selectUsePromotionDomain } from '../selectors';

describe('selectUsePromotionDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
